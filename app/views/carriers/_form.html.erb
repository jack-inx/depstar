<%= form_for(@carrier, :html => {:multipart => true}) do |f| %>
  <% if @carrier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carrier.errors.count, "error") %> prohibited this carrier from being saved:</h2>

      <ul>
      <% @carrier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  
  <div class="field">    
    <%= f.label :manufacturer %><br />
    <%= f.select :manufacturer_ids, Manufacturer.all.collect{|x|[x.name,x.id]}, {}, :multiple => true %>  
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
